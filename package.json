{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run root:start\"",
    "lint": "concurrently \"npm run root:lint\"",
    "root:start": "npm run start --prefix packages/root-config",
    "root:lint": "npm run lint --prefix packages/root-config"
  },
  "devDependencies": {
    "concurrently": "^6.2.2",
    "lerna": "^4.0.0"
  },
  "engines": {
    "npm": "Use yarn instead of NPM",
    "yarn": ">= 1.19.1"
  }
}
